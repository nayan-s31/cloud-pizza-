# Cloud Pizza

A Flask-based pizza ordering application deployed using Docker and Kubernetes.

## Technologies Used
- Python
- Flask
- Docker
- Kubernetes

## Run Locally

```bash
docker build -t cloud-pizza .
docker run -p 5000:5000 cloud-pizza
