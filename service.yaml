apiVersion: v1
kind: Service

metadata:
  name: cloud-pizza-service

spec:
  selector:
    app: pizza

  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30080

  type: NodePort